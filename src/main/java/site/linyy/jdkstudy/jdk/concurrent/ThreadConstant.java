package site.linyy.jdkstudy.jdk.concurrent;

public class ThreadConstant {

    {
        // 线程的优先级(1-10)
        int a = Thread.MIN_PRIORITY;// 1
        int b = Thread.NORM_PRIORITY;// 5 默认值
        int c = Thread.MAX_PRIORITY;// 10
    }

    {
    }

}
